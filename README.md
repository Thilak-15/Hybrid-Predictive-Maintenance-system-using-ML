# Hybrid-Predictive-Maintenance-system-using-ML
A simplified Predictive Maintenance system built using the CMAPSS jet engine dataset. The project classifies engine health into five stages, predicts the remaining useful life using regression models, and generates a combined risk score to trigger maintenance alerts. .
